# 🔧 故障排查指南

## 常见问题解决方案

### 1. 浮窗无法显示

**症状：** 点击"显示浮窗"按钮后，浮窗不出现

**解决方案：**
1. **刷新页面** - 这是最关键的步骤！
   - 按 `F5` 或 `Ctrl+R` 刷新当前页面
   - 扩展程序的内容脚本需要在页面加载时注入
   
2. **检查页面兼容性**
   - 某些特殊页面（chrome://, edge://, 扩展商店等）无法注入脚本
   - 建议在普通网站（如 Wikipedia、Reuters、Medium 等）测试

3. **重新加载扩展**
   - 打开 `chrome://extensions/`
   - 找到插件，点击刷新图标 🔄
   - 然后刷新测试页面

4. **查看控制台**
   - 按 `F12` 打开开发者工具
   - 查看 Console 标签是否有错误
   - 截图发送给我帮忙分析

---

### 2. 导出功能报错

**症状：** 点击"导出全部词句"时提示错误

**原因：** 旧版本缺少 `downloads` 权限

**解决方案：**
1. 确保使用的是最新版本（已添加 downloads 权限）
2. 完全卸载旧版本后再安装新版本：
   ```
   chrome://extensions/ 
   → 找到插件 
   → 点击"移除" 
   → 重新加载新版本
   ```

---

### 3. 清空按钮无效，高亮仍然存在

**症状：** 点击清空后，数字变为0，但页面上的黄色高亮还在

**原因：** 
- 清空时当前页面没有刷新，只清除了存储
- 需要通过消息通知当前页面清除高亮

**解决方案：**
1. 使用最新版本（已修复此问题）
2. 清空后刷新页面，高亮会消失
3. 或者使用浮窗中的清空按钮（会同时清除高亮）

---

### 4. 划词后没有反应

**症状：** 选中文本后，既没有高亮也没有收集

**可能原因：**
1. **页面未刷新** - 扩展安装后需要刷新页面
2. **选择了特殊元素** - 在输入框、文本域中无法高亮
3. **页面限制** - 某些网站可能有 CSP 策略限制

**解决方案：**
1. 刷新页面后再试
2. 在普通段落文本上测试
3. 使用提供的 test.html 测试页面验证功能

---

## ✅ 完整安装流程

### 第一步：卸载旧版本（如果有）
1. 打开 `chrome://extensions/`
2. 找到"英文划词收集器"
3. 点击"移除"按钮

### 第二步：安装新版本
1. 解压下载的 ZIP 文件
2. 打开 `chrome://extensions/`
3. 开启右上角"开发者模式"
4. 点击"加载已解压的扩展程序"
5. 选择解压后的文件夹

### 第三步：验证安装
1. 查看扩展列表中是否有插件图标
2. 确认插件状态为"已启用"

### 第四步：测试功能
1. 打开插件文件夹中的 `test.html`
2. 选择任意文本
3. 应该看到：
   - ✅ 文本变黄色高亮
   - ✅ 顶部出现"已收集"提示
   - ✅ 右上角出现浮窗

### 第五步：在真实网站使用
1. 访问任意英文网站（如 https://en.wikipedia.org）
2. **重要：刷新页面！**
3. 开始选择文本测试

---

## 📋 功能使用说明

### 浮窗操作
- **拖动浮窗**：点击并拖动顶部紫色区域
- **最小化**：点击 ➖ 按钮
- **关闭**：点击 ✖️ 按钮（数据不会丢失）
- **单个删除**：点击每条记录的"删除"按钮
- **批量导出**：点击 📥 按钮
- **清空全部**：点击 🗑️ 按钮

### Popup 面板操作
- **显示浮窗**：在当前页面显示浮窗
- **导出全部**：下载所有收集的词句为 txt 文件
- **清空所有**：删除所有数据并清除页面高亮

---

## 🐛 调试技巧

### 查看控制台日志
1. 按 `F12` 打开开发者工具
2. 切换到 Console 标签
3. 选择文本后应该看到：
   ```
   Word Collector Extension loaded successfully!
   Selected text: [你选择的文本]
   ```

### 查看存储数据
在控制台输入：
```javascript
chrome.storage.local.get(['phrases'], (result) => {
  console.log('Stored phrases:', result.phrases);
});
```

### 手动触发浮窗
在控制台输入：
```javascript
chrome.runtime.sendMessage({action: 'showWindow'});
```

---

## 🆘 仍然无法解决？

请提供以下信息：
1. Chrome 浏览器版本
2. 操作系统
3. 完整的控制台错误截图
4. 详细的操作步骤

---

## 📞 支持渠道
- GitHub Issues
- 邮件支持

最后更新：2025-11-15
版本：v1.0.2
